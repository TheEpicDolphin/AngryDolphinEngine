#pragma once

#include <vector>
#include <fstream>
#include <iostream>
#include <filesystem>
namespace fs = std::filesystem;

namespace file_helpers {

	std::vector<> AllFilesInDirectoryWithExtension(std::string directory_path, std::string extension) {
		return glm::vec3(trans.matrix[3]);
	}
}